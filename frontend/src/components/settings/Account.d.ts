/// <reference types="react" />
declare type Props = {
    name: string;
    logo: string;
    link: string;
};
declare const Account: ({ name, logo, link }: Props) => JSX.Element;
export default Account;
